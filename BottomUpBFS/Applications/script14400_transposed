#PBS -S /bin/bash
#PBS -q regular
#PBS -l mppwidth=14400
#PBS -l walltime=00:30:00
#PBS -j eo
#PBS -V

export MPICH_RANK_REORDER_DISPLAY=1
export MPICH_RANK_REORDER_METHOD=3
module load ipm
export IPM_LOGDIR="."
export IPM_LOG="full"
cd $SCRATCH
aprun -n 14400 ./graph500_withipm Force 32 FastGen
