Add the below line
#include <thrust/detail/config/compiler_fence.h>  // @OGUZ-EDIT header included
to the file
cusp/system/cuda/detail

changed line in file
cusplibrary/cusp/system/cuda/detail/multiply/csr_block_spmv.h
int num_cols = min(THREADS_PER_VECTOR,static_cast<unsigned int>(row_end - jj));

compilation command
nvcc -std=c++14 -ccbin=mpiCC test2.cu -I.. -I${CUDAPATH}/include -L${CUDAPATH}/lib64 -lcudart -o test2
